# Installation

## Manual installation

Copy the complete folder:

```bash
skills/pqd-skills
```

into your local Skills directory or the agent workspace that supports skill loading.

## Important

Do not copy only `SKILL.md`. The files under `references/` define the workflow, data integrity rules, figure standards, and writing conventions.
